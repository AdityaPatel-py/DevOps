version: '3'
services:
  frontend:
    build: ./frontend
    ports:
      - "80:80"

  backend:
  build: ./backend
  ports:
    - "5000:5000"
  environment:
    DB_HOST: mydb.cx0wucoeqd4u.ap-south-1.rds.amazonaws.com
    DB_USER: admin
    DB_PASSWORD: mypassword
    DB_NAME: mydb
